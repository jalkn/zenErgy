---
---
<div id="page-bg-overlay" class="fixed inset-0 z-[-1] transition-all duration-[600ms] ease-[cubic-bezier(0.4,0,0.2,1)]">
  <div class="absolute inset-0 bg-[radial-gradient(circle_at_50%_30%,_rgba(37,99,235,0.22)_0%,_rgba(15,23,42,0.75)_60%,_#030712_100%)]"></div>
  <div class="absolute inset-0 bg-gradient-to-b from-black/15 via-transparent to-black/25 pointer-events-none"></div>
  
  <!-- Lienzo donde se renderiza la matriz matemática -->
  <canvas id="quantum-mesh-canvas" class="absolute inset-0 w-full h-full block opacity-45"></canvas>
</div>

<style>
  #page-bg-overlay {
    will-change: filter, opacity;
  }
</style>

<script>
  // Inicialización de la matriz cuando el DOM esté listo
  const initQuantumMesh = () => {
    const canvas = document.getElementById('quantum-mesh-canvas') as HTMLCanvasElement;
    if (!canvas) return;

    const ctx = canvas.getContext('2d');
    if (!ctx) return;

    let points: Array<{ x: number; y: number; ox: number; oy: number; vx: number; vy: number }> = [];
    const spacing = 32; // Densidad de la malla original
    const mouse = { x: -1000, y: -1000, rx: -1000, ry: -1000, active: false };
    const radius = 130; // Radio de distorsión interactiva
    let animationFrameId: number;

    const resizeCanvas = () => {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
      setupPoints();
    };

    const setupPoints = () => {
      points = [];
      const cols = Math.ceil(canvas.width / spacing) + 1;
      const rows = Math.ceil(canvas.height / spacing) + 1;

      for (let i = 0; i < cols; i++) {
        for (let j = 0; j < rows; j++) {
          const x = i * spacing;
          const y = j * spacing;
          points.push({
            x: x, y: y,
            ox: x, oy: y,
            vx: 0, vy: 0
          });
        }
      }
    };

    const animate = () => {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      
      // Suavizado del puntero táctico
      mouse.rx += (mouse.x - mouse.rx) * 0.12;
      mouse.ry += (mouse.y - mouse.ry) * 0.12;

      ctx.fillStyle = 'rgba(6, 182, 212, 0.35)'; // cyan-500 matizado estilo HUD

      for (let i = 0; i < points.length; i++) {
        const p = points[i];
        const dx = mouse.rx - p.ox;
        const dy = mouse.ry - p.oy;
        const dist = Math.sqrt(dx * dx + dy * dy);

        if (dist < radius) {
          const force = (radius - dist) / radius;
          const angle = Math.atan2(dy, dx);
          // Fuerza de repulsión matemática
          const tx = p.ox - Math.cos(angle) * force * 24;
          const ty = p.oy - Math.sin(angle) * force * 24;
          p.vx += (tx - p.x) * 0.08;
          p.vy += (ty - p.y) * 0.08;
        } else {
          // Retorno aséptico al punto de equilibrio original
          p.vx += (p.ox - p.x) * 0.05;
          p.vy += (p.oy - p.y) * 0.05;
        }

        p.vx *= 0.82;
        p.vy *= 0.82;
        p.x += p.vx;
        p.y += p.vy;

        // Renderizar punto clásico estilo halftone
        ctx.beginPath();
        ctx.arc(p.x, p.y, 1.1, 0, Math.PI * 2);
        ctx.fill();
      }

      animationFrameId = requestAnimationFrame(animate);
    };

    // Listeners del Ecosistema
    window.addEventListener('resize', resizeCanvas);
    
    window.addEventListener('mousemove', (e) => {
      mouse.x = e.clientX;
      mouse.y = e.clientY;
      mouse.active = true;
    });

    window.addEventListener('mouseleave', () => {
      mouse.x = -1000;
      mouse.y = -1000;
      mouse.active = false;
    });

    // Encendido inicial
    resizeCanvas();
    animate();
  };

  // Ejecutar en la carga inicial
  initQuantumMesh();

  // Asegurar compatibilidad con las transiciones de Astro (View Transitions)
  document.addEventListener('astro:page-load', initQuantumMesh);
</script>